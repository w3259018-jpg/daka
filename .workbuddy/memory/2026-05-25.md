# 2026-05-25 工作记录

## 后端部署验证
- 后端已部署到微信云托管，域名 https://weiyidaka.online 正常运行
- 测试结果：GET /api/me 返回401（JWT认证正常），POST /api/login 返回 {"err":"code required"}（接口正常）
- HTTPS证书正常工作，响应头包含 server: cbrgw，确认为微信云托管

## 待解决问题清单（按优先级排列）

### 问题1：小程序API地址指向内网IP ✅ 已解决
- 杰哥已将 config.js 第16行改为 `const apiBase = PROD_API_BASE;`
- 现在所有环境统一走 https://weiyidaka.online

### 问题2：wx:key 绑定对象（次要）
- 现象：控制台警告 "Do not set same key "[object Object]" in wx:key"
- 根因：tasks.wxml 第22行 week 数组的 wx:key="*this"，item 是对象被转成 "[object Object]"
- 解决方向：改成 wx:key="index"

### 问题3：微信后台服务器域名配置 ✅ 已解决
- 已配置：request/uploadFile/downloadFile 三个合法域名均为 https://weiyidaka.online
- ICP备案已确认通过

### 问题4：登录接口500 Internal Server Error（新增关键问题）
- 现象：POST /api/login 返回500，前端无限重试后 timeout
- 排查阶段：已在 index.js 添加诊断日志（Node版本 + fetch可用性）
- 已在 login catch 中添加了详细错误输出
- 待杰哥重新部署后查看云托管运行日志定位根因
